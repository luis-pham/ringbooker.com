import { UserBookingsLive } from '@/components/user/user-bookings-live';

export const metadata = {
  title: 'Bookings and calendar flow.',
};

export default function UserBookingsPage() {
  return <UserBookingsLive />;
}

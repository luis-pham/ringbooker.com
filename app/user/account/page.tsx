import { UserAccountLive } from '@/components/user/user-account-live';

export const metadata = {
  title: 'Account · RingBooker',
};

export default function UserAccountPage() {
  return <UserAccountLive />;
}

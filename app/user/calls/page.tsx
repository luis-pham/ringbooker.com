import { UserCallsLive } from '@/components/user/user-calls-live';

export const metadata = {
  title: 'Calls, transcripts, and missed revenue recovery.',
};

export default function UserCallsPage() {
  return <UserCallsLive />;
}

import { MarketingContactTemplate } from '@/components/marketing/marketing-contact';

export const metadata = {
  title: 'Book a RingBooker Demo',
  description: 'Book a RingBooker demo for your salon, spa, med spa, or beauty clinic. See how AI phone answering can recover after-hours, overflow, and missed booking calls on your current number.',
};

export default function ContactPage() {
  return <MarketingContactTemplate />;
}
